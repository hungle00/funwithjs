<section class='main-section' id='configuration'>
    <header><h1>WELCOME</h1></header>
    <h2 id='cf'>Config File</h2>
    <p>Your project structure is probably like this:</p>
                  
    <p>
        Disable click chuột phải
    </p>
                  
    <div class= 'code'>
        <code>
          $(document).bind("contextmenu",function(e){<br>
            //Trong này có thể viết hàm xử lý gì đó tùy bạn<br>
            // Return false để loại bỏ thao tác với chuột phải<br>
            return false;<br>
        });</code>       
    </div>
                          
    <h2 id='tc'>Conclusion</h2>
    <p>
        Again. You might not need jQuery. But if you’re aggressively removing or avoiding
        it as an optimisation, make sure it’s not a premature one.
    </p>
</section> 